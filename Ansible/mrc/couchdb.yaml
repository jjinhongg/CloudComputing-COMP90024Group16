# Group 16

# Team members:

# Zenan Ji (Student ID: 1122396) - city: Nanjing

# Weijie Ye (Student ID: 1160818) - city: Fuzhou

# Wenqin Liu (Student ID: 807291) - city: Guangdong

# Jinhong Yong (Student ID: 1198833) - city: Kuala Lumpur

# Zixuan Zeng (Student ID: 1088297) - city: Melbourne

# Set up CouchDB using Docker Containers
- hosts: DataNodes
  vars_files:
    - host_vars/couchdb_settings.yaml
  gather_facts: true

  roles:
    - role: couchdb-setup

 # Perform CouchDB configuration on all hosts except Masternode
- hosts: DataNodes:!Masternode
  vars_files:
    - host_vars/couchdb_settings.yaml
  gather_facts: true

  roles:
    - role: couchdb-finish-setup

 # Perform CouchDB replicate on masternode
# - hosts: DataNodes
#   vars:
#     current_ip: "{{hostvars[inventory_hostname]['ansible_default_ipv4']['address']}}"
#     masternode: "{{hostvars[groups['DataNodes'][0]]['ansible_default_ipv4']['address']}}" 
#   vars_files:
#     - host_vars/couchdb_settings.yaml
#   gather_facts: true

#   roles:
#     - role: couchdb-replicate